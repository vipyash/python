x = 120
rev = 0

# Save the sign
sign = -1 if x < 0 else 1

# Work with absolute value
x = abs(x)

while x > 0:
    digit = x % 10
    rev = (rev * 10) + digit
    x = x // 10

# Restore the sign
rev = rev * sign

print(rev)
